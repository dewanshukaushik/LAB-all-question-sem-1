#include<stdio.h>
#include<conio.h>
typedef struct polynomial
{
int coe;
int pow;
struct polynomial*next;

}node;
void add polynomial(node*ptr1,node*ptr3,node*ptr3)
{
int powe;
float coef;
while((ptr1!=NULL)&&(ptr2!=NULL))
{
if (ptr1->power > ptr2->power)
{
powe=ptr->power
coef=ptr1->coeff;
ptr1=ptr1->next;
}
elseif (ptr->power<ptr2->power)
power=ptr2->power;
coef=ptr2->coff;
ptr2=ptr2->next;
}
else
coef=ptr1->coef+ptr2->coff;
powe=ptr1->power;
ptr1=ptr1->next;
ptr2=ptr2->next;
}
if(coef!=0)
addnode(ptr3,coef,powe);
}
if(ptr==NULL){
for(;ptr2!=NULL;ptr2=ptr2->next)
adnode(ptr3,ptr2->coeff,ptr2->power);
}
if(ptr2==NULL){
for(;ptr!=NULL;ptr2=ptr->next)
addnode(ptr3,ptr2->coeff,ptr2->power
}
if|(ptr==NULL){
for(;ptr1!=NULL;ptr1=ptr1->next)
addnode(ptr3;ptr1->coeff,ptr1->power)
}