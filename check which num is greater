#include<stdio.h>
int main()
{
int a,b,c;
printf("enter a number ");
scanf("%d%d%d",&a,&b,&c);
if(a>b && a>c)
  printf("a is greater");
  if(b>c)
  printf("b is greater");
  if (c>b)
  printf("c is greater");
  return(0);
}
