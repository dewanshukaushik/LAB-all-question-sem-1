#include <stdio.h>
char main() {
char name[]={'K','I','E','T'};
char name2[]={"KIET"};
 int a,s,d,f;
   a = sizeof(name);
   s = sizeof(name2);
   d = strlen(name);
   f = strlen(name2);
     
     printf("%d %d %d %d",a,s,d,f);

    return(0);
}
